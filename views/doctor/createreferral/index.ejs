<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Follow-Up Referral</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body class="bg-gray-100">
    <nav class="bg-blue-600 text-white p-4">
        <div class="container mx-auto flex justify-between items-center">
            <div class="space-x-4 flex items-center">
                <a href="/doctor/dashboard" class="text-white hover:text-gray-200" style="margin-left: 5px;">
                    <i class="fas fa-home"></i>
                </a>
            </div>
            <div class="space-x-4 flex items-center">
                <h1 class="text-2xl font-bold">Create Follow-Up Referral</h1>
            </div>
            <div class="space-x-4">
                <span class="font-medium"><%= username %></span>
                <a href="/logout" class="hover:text-gray-200">Logout</a>
            </div>
        </div>
    </nav>

    <div class="container mx-auto p-8">
        <div class="bg-white p-6 rounded-lg shadow-lg">
            <h2 class="text-xl font-semibold mb-4">Create Follow-Up Referral</h2>

            <% if (referralExists) { %>
                <div class="text-red-600">
                    <strong>Follow-up referral has already been created for this appointment.</strong>
                </div>
            <% } %>
            <form action="/doctor/createreferral/<%= appointment.AppointmentID %>" method="POST">
                <div class="mb-4">
                    <label for="purpose" class="block text-sm font-medium">Purpose of Referral</label>
                    <input type="text" id="purpose" name="purpose" class="border rounded w-full py-2 px-3" required>
                </div>
                <div class="mb-4">
                    <label for="nextVisitDate" class="block text-sm font-medium">Next Visit Date</label>
                    <input type="date" id="nextVisitDate" name="nextVisitDate" class="border rounded w-full py-2 px-3" required>
                </div>
                <div class="mb-4">
                    <label for="nextVisitTime" class="block text-sm font-medium">Next Visit Time</label>
                    <input type="time" id="nextVisitTime" name="nextVisitTime" class="border rounded w-full py-2 px-3" required>
                </div>
                <button type="submit" class="p-2 rounded mt-4 <%= referralExists ? 'bg-gray-400 cursor-not-allowed' : 'bg-blue-600 hover:bg-blue-700 text-white' %>" <%= referralExists ? 'disabled' : '' %>>
                    Create Follow-Up Referral
                </button>
            </form>
        </div>
    </div>
</body>
</html>
