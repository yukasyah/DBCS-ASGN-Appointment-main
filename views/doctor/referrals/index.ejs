<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Referrals</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body class="bg-gray-100">
    <nav class="bg-blue-600 text-white p-4">
        <div class="container mx-auto flex justify-between items-center">
            <div class="space-x-4 flex items-center">
                <a href="/doctor/dashboard" class="text-white hover:text-gray-200" style="margin-left: 5px;">
                    <i class="fas fa-home"></i>
                </a>
            </div>
            <div class="space-x-4 flex items-center">
                <h1 class="text-2xl font-bold">View Referrals</h1>
            </div>
            <div class="space-x-4">
                <span class="font-medium"><%= username %></span>
                <a href="/logout" class="hover:text-gray-200">Logout</a>
            </div>
        </div>
    </nav>

    <div class="container mx-auto p-8">
        <div class="bg-white p-6 rounded-lg shadow-lg">
            <h2 class="text-xl font-semibold mb-4">Referrals</h2>

            <% if (referrals.length === 0) { %>
                <p>No referrals created yet.</p>
            <% } else { %>
                <table class="min-w-full bg-white border border-gray-300">
                    <thead>
                        <tr>
                            <th class="px-4 py-2 text-left">Referral ID</th>
                            <th class="px-4 py-2 text-left">Patient Name</th>
                            <th class="px-4 py-2 text-left">Referral Date</th>
                            <th class="px-4 py-2 text-left">Status</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% referrals.forEach(referral => { %>
                            <tr class="border-b">
                                <td class="px-4 py-2"><%= referral.ReferralID %></td>
                                <td class="px-4 py-2"><%= referral.PatientFullName %></td>
                                <td class="px-4 py-2"><%= referral.CreatedDate %> <%= referral.CreatedTime %></td>
                                <td class="px-4 py-2"><%= referral.ReferralStatus %></td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>
            <% } %>
        </div>
    </div>
</body>
</html>
