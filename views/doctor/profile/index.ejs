<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Doctor Profile</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
</head>
<body class="bg-gray-100">
    <nav class="bg-blue-600 text-white p-4">
        <div class="container mx-auto flex justify-between items-center">
            <div class="space-x-4 flex items-center">
                <a href="/doctor/dashboard" class="text-white hover:text-gray-200" style="margin-left: 5px;">
                    <i class="fas fa-home"></i>
                </a>
            </div>
            <div class="space-x-4 flex items-center">
                <h1 class="text-2xl font-bold">Doctor Profile</h1>
            </div>
            <div class="space-x-4">
                <span class="font-medium"><%= username %></span>
                <a href="/logout" class="hover:text-gray-200">Logout</a>
            </div>
        </div>
    </nav>

    <div class="container mx-auto p-8">
        <div class="bg-white p-6 rounded-lg shadow-lg">
            <h2 class="text-xl font-semibold mb-4">Doctor Information</h2>

            <div class="mb-4">
                <label class="block text-sm font-medium text-gray-700">Doctor Name</label>
                <p class="mt-1 text-gray-900"><%= doctorDetails.DoctorName || 'N/A' %></p>
            </div>

            <div class="mb-4">
                <label class="block text-sm font-medium text-gray-700">Phone Number</label>
                <p class="mt-1 text-gray-900"><%= doctorDetails.DoctorPhoneNum || 'N/A' %></p>
            </div>

            <div class="mb-4">
                <label class="block text-sm font-medium text-gray-700">Department</label>
                <p class="mt-1 text-gray-900"><%= doctorDetails.DepartmentName || 'N/A' %></p>
            </div>
        </div>
    </div>
</body>
</html>
